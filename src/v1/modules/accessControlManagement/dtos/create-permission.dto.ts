// import { ObjectLiteral } from "@shared/types/object-literal.type";

export type CreatePermission = {
  moduleId: string;
  name: string;
  description: string;
  action: string;
  slug: string;
};
